.DS_Store
__pycache__/
*.py[cod]
.pytest_cache/
tmp/

# Reproducible runtime and backtest artifacts stay local. Curated results live
# under reports/ so a normal `git add .` does not stage hundreds of CSV files.
output/

# Raw provider responses are large and can be rebuilt from their manifests.
data/**/_source_cache/

.env.local
.env.*.local
